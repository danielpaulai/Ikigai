# Anthropic (Claude)
ANTHROPIC_API_KEY=

# Supabase — public (safe for browser)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — server only (never expose to client; never commit real values)
SUPABASE_SERVICE_ROLE_KEY=
